import 'package:flutter/material.dart';

class PasswordPrompt extends StatelessWidget {
  final bool isNewBusiness;

  PasswordPrompt(this.isNewBusiness);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
